# ---------------------------------------------------------------------------
# FULL dependencies — enables LIVE MODE: real LLM agents, vector RAG, LangGraph.
# Install with:  pip install -r requirements-full.txt
# Then set OPENAI_API_KEY or ANTHROPIC_API_KEY in .env to switch from DEMO to LIVE.
# ---------------------------------------------------------------------------
-r requirements.txt

# Agentic orchestration
langgraph==0.2.60
langchain-core==0.3.29

# Vector database + RAG
chromadb==0.5.23

# LLM providers (either one enables LIVE mode)
openai==1.59.6
anthropic==0.42.0
httpx==0.27.2
